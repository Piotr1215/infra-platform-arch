---
apiVersion: azure.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: provider-azure
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: upbound-system
      name: azure-creds
      key: creds
